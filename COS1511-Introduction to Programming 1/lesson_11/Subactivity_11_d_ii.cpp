/*
Rewrite the following two if statements so that no if appears in them:
(i) if (age >= 18) mayVote = true;
     else
        mayVote = false;
*/

    //myVote = age >= 18;

/*
(ii) if (length > 1.8 && weight < 70) overWeight = false;
     else
        overWeight = true;
*/

    // overWeight = !(length > 1.8 && weight < 70);