/*
Rewrite the following if statements as single assignment statements:
 (i) if (grade > 7) highSchool = true;
   else
      highSchool = false;

(ii) if (age < 13 || age > 19)
teenager = false;
     else
        teenager = true;

(iii) if (x < 0)
found = false;
     else if (x % 4 == 0)
        found = true;
     else
        found = false;
*/

// highSchool = (grade > 7);

// teenager = !(age < 13 || age >19);

// found = (x >= 0 && x % 4 == 0);

